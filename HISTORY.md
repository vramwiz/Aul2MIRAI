# Aul2MIRAI 開発・検証履歴

完了済みの実装と実機確認結果を日付順に記録する。現在の方針と未解決課題は[`note.md`](note.md)、通信仕様は[`PIPE_INTERFACE.md`](PIPE_INTERFACE.md)、確認済みの生成用エイリアスは[`ALIAS_CATALOG.md`](ALIAS_CATALOG.md)を参照する。

## 2026-07-23

- エイリアス再生成をフィルター追加の基礎として利用できるか検証するため、複製元と生成物を同じ編集コールバック内で詳細比較する処理を追加した。比較対象は長さ、表示名、種類、素材パス、エフェクト順、全設定値、有効・ロック状態、中間点の相対位置、トラックバーの移動モード・内部パラメーター・補間フラグ・項目グループとした。配置、選択、フォーカス、AviUtl2による正規化後のエイリアスハッシュだけを比較対象外とする。
- 詳細比較で不一致または切り捨て設定値を検出した場合は、同じ編集コールバック内で生成済みオブジェクトを削除し、`create_verification_failed`で要求全体を失敗させる構成とした。
- AviUtl2 v2.1.0で標準の画像オブジェクトをレイヤー0・144～224フレームから空き位置のレイヤー1・300～380フレームへ再生成し、画像ファイルと標準描画の全比較項目が一致して`recreation_verification.verified=true`となることを確認した。
- 外部フィルター`サウンドエフェクター`を持つグループ制御(音声)を空きレイヤーへ再生成したところ、`Pitch: Rate(Hz)`が元の`64`から生成物の`80`へ変化することを検出した。要求は`create_verification_failed`となり、生成物が削除され、オブジェクト数と`state_token`が変更前のままであることを確認した。エイリアス再生成は全フィルターで無条件に同一性を保持しないため、将来のフィルター追加では再生成後の詳細比較を必須とする。
- `aup2\filter_sample.aup2`の図形オブジェクトを、レイヤー0・98～178フレームから空き位置のレイヤー1・98～178フレームへ再生成した。`図形`、`標準描画`、`色調補正`の順序と全設定値が一致し、初期値から変更された`色調補正.明るさ=110.00`も生成物に保持された。再取得でも生成物の明るさ`110.00`、コントラスト`100.00`を確認した。
- 最小サンプルから、連番のエフェクトブロックへ全設定項目を記述する生成規則と、非初期値を`create_object_from_alias`へ渡せることを確認した。確認済みブロックと値形式は[`ALIAS_CATALOG.md`](ALIAS_CATALOG.md)へ分離した。
- `duplicate_objects`の各要素へ任意の`repeat_effect`を追加した。指定した既存エフェクトの最後の同名ブロックを新しい連番ブロックとしてエイリアス末尾へ追加し、元スナップショットにも同じエフェクトを追加した期待値を作って生成直後に詳細比較する。
- 起動中のAviUtl2 v2.1.0で、`filter_sample.aup2`の選択図形をレイヤー1・98～178フレームへ`repeat_effect="色調補正"`付きで複製した。生成物は`図形`、`標準描画`、`色調補正`、`色調補正`の順となり、2段の色調補正はいずれも明るさ`110.00`、コントラスト`100.00`、有効、ロックなしであることを外部再取得で確認した。`recreation_verification.verified=true`に加え、ユーザーがAviUtl2画面上でも正常動作を確認した。
- `duplicate_objects`へ`replace_source`を追加した。`repeat_effect`との併用、1要求1件、元と同じ生成位置を必須とし、同じ編集コールバック内で元の完全取得、削除、追加後エイリアスの同位置生成、詳細比較を行う。生成または検証失敗時は元エイリアスを同位置へ復元し、復元物も詳細比較する。
- AviUtl2 v2.1.0の`filter_sample.aup2`で、選択図形を`replace_source=true`、`repeat_effect="色調補正"`によりレイヤー0・98～178フレームへ同位置置換した。処理前後ともオブジェクト数1で、選択・フォーカス、配置範囲を維持した。生成物の色調補正2段はいずれも明るさ`110.00`、コントラスト`100.00`、有効、ロックなしで、`recreation_verification.verified=true`を確認した。
- 同位置置換後の2段目の`色調補正`に対し、既存の設定値変更APIでコントラストを`100.00`から`110.00`へ変更した。プレビューで対象エフェクトindex 3、変更前後値を確認し、書込み直後の`verified=110.00`と外部再取得を確認した。1段目はコントラスト`100.00`のまま、2段目だけ`110.00`となり、同名フィルターをインデックス指定して個別変更できることを確認した。
- 生成知識を開発方針から分離し、`ALIAS_CATALOG.md`を新設した。図形、標準描画、色調補正の確認済みブロックと値形式、画像オブジェクトの生成確認、外部フィルター`サウンドエフェクター`の正規化事例、新規サンプルの追記形式を集約した。README、AI操作ガイド、通信仕様から参照し、配布ZIPにも含める構成とした。
- 未保存プロジェクト上の初期値動画をレイヤー0・63～143フレームからレイヤー1の同区間へ再生成した。`動画ファイル`、`映像再生`の順序、空の素材パス、全設定値、状態、中間点、トラック情報が一致し、`recreation_verification.verified=true`を確認した。
- `magnific_poRvrC5ehw.mp4`をD&Dした動画をレイヤー0・209～360フレームからレイヤー1の同区間へ再生成した。素材パス、`再生位置=0.000,5.084,再生範囲,0`、152フレームの長さを含む全比較項目が一致し、`recreation_verification.verified=true`を確認した。初期値動画との差と確認済みブロックを`ALIAS_CATALOG.md`へ登録した。
- `sample2.png`をD&Dした画像をレイヤー0・233～313フレームからレイヤー1の同区間へ再生成した。素材パス、`画像ファイル`、`標準描画`の順序、全設定値、状態、中間点、トラック情報が一致し、`recreation_verification.verified=true`を確認した。
- 未選択オブジェクトを後続操作の対象にできるよう、`preview_set_focus_object`と`set_focus_object`を追加した。最新の状態指紋、対象index、明示的な適用指定、編集コールバック内での現在フォーカス・配置範囲・内容ハッシュ再検証、変更後の`focused=true`再取得確認を必須とする。Win64 Debugの確認用出力先でコンパイルに成功した。
- 再ビルド後のAviUtl2 v2.1.0で、未選択だった初期値画像index 0へのフォーカス移動をプレビューしてから適用した。元のフォーカスindex 1からindex 0へ移動し、新しい状態指紋、`selected=true`、`focused=true`、`get_selected_objects`での全設定取得を確認した。続けてレイヤー0・117～197フレームからレイヤー1の同区間へ複製し、全比較項目の一致と`recreation_verification.verified=true`を確認した。初期値とD&D画像の確認済みブロックを`ALIAS_CATALOG.md`へ登録した。
- 未保存プロジェクト上のD&D音声`nc294512_オリジナル曲_風に抱かれて.wav`を、レイヤー0・323～2648フレームからレイヤー1の同区間へ再生成した。素材パス、`再生位置=0.000,77.539,再生範囲,0`、2326フレームの長さ、`音声ファイル`と`音声再生`の全比較項目が一致し、`recreation_verification.verified=true`を確認した。
- 未選択だった初期値音声index 0へ外部コマンドでフォーカスを移し、選択対象として取得した後、レイヤー0・153～233フレームからレイヤー1の同区間へ再生成した。空の素材パス、初期値の再生範囲、81フレームの長さを含む全比較項目が一致し、`recreation_verification.verified=true`を確認した。初期値とD&D音声の確認済みブロックを`ALIAS_CATALOG.md`へ登録した。
- `bugatti.obj`をD&Dしたモデルファイルをレイヤー1・344～424フレームからレイヤー2の同区間へ再生成した。素材パス、`座標系を調整=0`、`モデルファイル`と`標準描画`の全比較項目が一致し、`recreation_verification.verified=true`を確認した。
- 未選択だった初期値モデルへ外部コマンドでフォーカスを移し、レイヤー0・219～299フレームからレイヤー2の同区間へ再生成した。空の素材パスを含む全比較項目が一致し、`recreation_verification.verified=true`を確認した。初期値とD&Dモデルの確認済みブロックを`ALIAS_CATALOG.md`へ登録した。
- `モデルファイル`が`object_type=unknown`だったため、標準種類`model`として分類する処理を追加した。Win64 Debugビルドに成功し、実機応答の確認は再起動後に行う。
- 本文`123ABC漢字`のD&Dテキストをレイヤー0・294～374フレームからレイヤー1の同区間へ再生成した。D&D元の外部素材パスはなく、本文が`テキスト`項目へ埋め込まれていた。本文、`テキスト`と`標準描画`の順序、全設定値、状態、中間点、トラック情報が一致し、`recreation_verification.verified=true`を確認した。
- 未選択だった本文空の新規テキストへ外部コマンドでフォーカスを移し、レイヤー0・158～238フレームからレイヤー1の同区間へ再生成した。全初期値と81フレームの長さを含む比較項目が一致し、`recreation_verification.verified=true`を確認した。新規とD&Dテキストの確認済みブロックを`ALIAS_CATALOG.md`へ登録した。
- 図形の種類リスト順に、`背景`、`円`、`四角形`、`三角形`、`五角形`、`六角形`、`星型`、`ハート`の8種類をレイヤー0へ配置して取得した。新規作成時の初期値はリスト先頭の背景ではなく円であることをサンプル条件として記録した。
- 8種類の図形を外部フォーカス移動で順番に対象化し、元と同じフレーム範囲のレイヤー1へ再生成した。全種類で`図形の種類`以外の値は共通し、8件すべて全比較項目が一致して`recreation_verification.verified=true`となった。種類リスト順、初期値、確認済みブロックを`ALIAS_CATALOG.md`へ登録した。
- シーンオブジェクトについて、映像・音声一体型、分離映像、分離音声の3構成を取得した。各構成に`シーン=0`・再生範囲0秒・81フレームの未指定状態と、`シーン=1`・再生範囲3.934秒・118フレームの指定済み状態があり、計6オブジェクトだった。一体型は`映像再生`内に音量・左右を持ち、分離型は`標準描画`と`音声再生`の別オブジェクトだった。
- 一体型2件をレイヤー4、分離映像2件をレイヤー5、分離音声2件をレイヤー6の元と同じフレーム範囲へ再生成した。6件すべて個体単位の全比較項目が一致し、`recreation_verification.verified=true`を確認した。分離ペアは同じシーン番号と配置関係を保持したが、公開SDKで取得できない複数オブジェクト間の内部リンクは確認対象外とした。確認済みブロックを`ALIAS_CATALOG.md`へ登録した。
- 新規追加した初期値フレームバッファをレイヤー0・85～165フレームからレイヤー1の同区間へ再生成した。`アルファチャンネルを維持=1`、`フレームバッファをクリア=0`、`基本出力`の全設定値、状態、中間点、トラック情報が一致し、`recreation_verification.verified=true`を確認した。初期値の確認済みブロックを`ALIAS_CATALOG.md`へ登録した。
- 新規追加した初期値オーディオバッファをレイヤー0・124～204フレームからレイヤー1の同区間へ再生成した。`オーディオバッファをクリア=1`、`音声再生.音量=100.00`、`左右=0.00`、状態、中間点、トラック情報が一致し、`recreation_verification.verified=true`を確認した。画像用フレームバッファと区別して、初期値の確認済みブロックを`ALIAS_CATALOG.md`へ登録した。
- `オーディオバッファ`が`object_type=unknown`だったため、標準種類`audio_buffer`として分類する処理を追加した。Win64 Debugビルドに成功し、実機応答の確認は再起動後に行う。
- 新規追加した初期値の直前オブジェクトをレイヤー0・157～237フレームからレイヤー1の同区間へ再生成した。本体の`直前オブジェクト`には設定項目がなく、`標準描画`だけが設定値を持っていた。全比較項目が一致し、`recreation_verification.verified=true`を確認した。参照元となる別オブジェクトを使った表示挙動は未確認として、確認済みブロックを`ALIAS_CATALOG.md`へ登録した。
- `直前オブジェクト`が`object_type=unknown`だったため、標準種類`previous_object`として分類する処理を追加した。Win64 Debugビルドに成功し、実機応答の確認は再起動後に行う。
- 初期値の円を本体とするパーティクル出力をレイヤー0・182～262フレームからレイヤー1の同区間へ再生成した。`図形`と`パーティクル出力`の順序、図形設定、パーティクル固有の全設定値、状態、中間点、トラック情報が一致し、`recreation_verification.verified=true`を確認した。
- パーティクル出力で選択できる図形種類は通常図形と同じ8種類であることをユーザーの配置条件として記録した。図形名自体は通常図形で全8種類の生成確認済みだが、パーティクル出力との組み合わせで再生成比較したのは円だけとし、確認範囲を`ALIAS_CATALOG.md`へ明記した。
- 新規追加した初期値の部分フィルタをレイヤー0・227～307フレームからレイヤー1の同区間へ再生成した。`部分フィルタ`単独のエフェクト構成、円マスクの全設定値、有効・ロック状態、中間点、トラック情報が一致し、`recreation_verification.verified=true`を確認した。
- 画面で確認したマスク候補11項目と初期値ブロックを`ALIAS_CATALOG.md`へ登録した。生成比較済みは円だけとし、後続フィルターを使った実際の表示挙動、反転、他のマスク種類、シーン・仮想バッファ・ファイル指定は未確認として区別した。
- `部分フィルタ`が`object_type=unknown`だったため、標準種類`partial_filter`として分類する処理を追加した。Win64 Debugビルドに成功し、実機応答の確認は再起動後に行う。
- 新規追加した初期値の`グループ制御`をレイヤー0・257～337フレームからレイヤー1の同区間へ再生成した。位置XYZ、回転XYZ、拡大率、対象レイヤー数を含む全設定値、状態、中間点、トラック情報が一致し、`recreation_verification.verified=true`を確認した。
- 新規追加した初期値の`グループ制御(音声)`をレイヤー0・421～501フレームからレイヤー1の同区間へ再生成した。音量、左右、対象レイヤー数を含む全設定値、状態、中間点、トラック情報が一致し、`recreation_verification.verified=true`を確認した。両オブジェクトの確認済みブロックと未確認の作用範囲を`ALIAS_CATALOG.md`へ登録した。
- `グループ制御(音声)`が映像用と同じ`group_control`に分類されていたため、音声用を`audio_group_control`として先に完全一致で分類する処理を追加した。Win64 Debugビルドに成功し、実機応答の確認は再起動後に行う。
- 新規追加した初期値の`カメラ制御`をレイヤー0・150～230フレームからレイヤー1の同区間へ再生成した。カメラ位置、目標位置、対象・焦点レイヤー、傾き、深度ぼけ、視野角、対象レイヤー数を含む全設定値と状態、トラック情報が一致し、`recreation_verification.verified=true`を確認した。
- 新規追加した初期値の`時間制御(オブジェクト)`をレイヤー0・256～336フレームからレイヤー1の同区間へ再生成した。`位置=0.000,100.000,直線移動(時間制御),0`、繰り返し、コマ落ち、対象レイヤー数を含む全比較項目が一致し、`recreation_verification.verified=true`を確認した。
- 新規追加した初期値の`画像合成(オブジェクト)`をレイヤー0・358～438フレームからレイヤー1の同区間へ再生成した。本体の対象レイヤー数と後段の標準描画、通常の合成モードを含む全比較項目が一致し、`recreation_verification.verified=true`を確認した。画像で確認した共通合成モード13候補は、生成比較済みの通常と区別して`ALIAS_CATALOG.md`へ登録した。
- `時間制御(オブジェクト)`と`画像合成(オブジェクト)`が`unknown`だったため、それぞれ`time_control`、`image_composition`として分類する処理を追加した。Win64 Debugビルドに成功し、実機応答の確認は再起動後に行う。
- カスタムオブジェクト8種類として、音声波形表示、集中線、カウンター、レンズフレア、雲、星、雪、雨の初期値と標準描画を取得した。前半5種類は81フレーム、星・雪・雨は161フレームだった。
- 8種類を外部フォーカス移動で順番に対象化し、レイヤー0からレイヤー1の同区間へ再生成した。各カスタム本体と標準描画の順序、全設定値、有効・ロック状態、中間点、トラック情報がすべて一致し、8件とも`recreation_verification.verified=true`となった。
- 選択式の`形状`は図形と同じ8種類で初期値も円であることをユーザー確認として記録した。レンズフレア、星、雪では実際に`形状=円`を取得・生成比較したが、他形状との組み合わせは未確認とした。雲の`形状=4.0`は数値トラックなので別項目として扱った。
- カスタムオブジェクト名は導入スクリプトにより増減し得るため、固定の種類分類は追加せず、`object_type=unknown`と`primary_effect`で識別する方針を`ALIAS_CATALOG.md`と`PIPE_INTERFACE.md`へ記録した。
- 残りのカスタムオブジェクト8種類として、ランダム小物配置(カメラ制御)、ライン(移動軌跡)、扇型、多角形、フレア、水面、値で図形、連結ラインの初期値と標準描画を取得した。8種類とも161フレームだった。
- 8種類を外部フォーカス移動で順番に対象化し、レイヤー0からレイヤー1の同区間へ再生成した。各カスタム本体と標準描画の順序、全設定値、有効・ロック状態、中間点、トラック情報がすべて一致し、8件とも`recreation_verification.verified=true`となった。
- ランダム小物配置(カメラ制御)の固有形状候補として、球形、三角形、四角形、星型、破片、立方体、ひし形の7種類をユーザー提供画像から記録した。実機取得・生成確認した初期値は`形状=球形`、`ランダム色=0`であり、通常図形の種類体系とは分けて`ALIAS_CATALOG.md`へ登録した。
- カメラ、移動軌跡、対象レイヤーを参照する可能性があるカスタムオブジェクトは、単体エイリアスの再生成一致だけを確認済みとし、参照対象との実際の連携挙動は未確認として記録した。
- 標準の画面効果として`走査線`と`周辺ボケ光量`を取得した。両方とも画面効果本体と標準描画の構成で、本体は有効・ロックなし、標準描画は有効・ロックありだった。
- 走査線をレイヤー0・0～400フレームから、周辺ボケ光量をレイヤー0・401～801フレームから、それぞれレイヤー1の同区間へ再生成した。両方とも401フレーム、全設定値、状態、中間点、トラック情報が一致し、`recreation_verification.verified=true`となった。
- 2種類を共通の外部向け種類`screen_effect`として分類し、具体的な画面効果名は`primary_effect`で区別する処理を追加した。Win64 Debugビルドに成功し、分類名の実機応答は再起動後に確認する。
- ユーザー確認により標準オブジェクトの初回学習対象がすべて完了した。標準・カスタムオブジェクトの初期値学習を一巡したものとして、次は標準フィルター、その後に外部フィルタープラグインへ進む方針を`note.md`へ記録した。
- AviUtl2がフィルター単体の独立オブジェクトを扱えることを、赤い円とその直下に配置された色調補正サンプルで確認した。正式な先頭効果名は`フィルタオブジェクト`で、設定項目のないロック済み本体の後ろに通常の`色調補正`ブロックが続いていた。
- 元の円はレイヤー1・1095～1495フレーム、フィルタオブジェクトはレイヤー2・1230～1630フレームで、重複区間に色調補正が作用することをユーザーが画面上で確認した。円は`色=ff0000`、色調補正は非初期値の`明るさ=51.60`、`コントラスト=88.30`だった。
- フィルタオブジェクトをレイヤー4、赤い円をレイヤー3の元と同じ時間範囲へ再生成し、レイヤー差と時間位置を保持した。両方とも全設定値、状態、中間点、トラック情報が一致し、個体単位で`recreation_verification.verified=true`となった。複製ペアの画面上の作用と一般的な対象決定規則は未確認とした。
- `フィルタオブジェクト`を外部向け種類`filter_object`へ分類する処理を追加した。Win64 Debugビルドに成功し、分類名の実機応答は再起動後に確認する。
- フィルター追加方針を、対象削除と同位置再生成による内部フィルター追加、および対象を削除しない独立フィルタオブジェクト配置の二方式へ更新した。後者はSDKから作用対象の関連情報を取得できないため、配置規則を追加検証してから一般機能化する。
- 独立フィルター付きの赤い円と、円自身に色調補正を持つ構成を横に並べたサンプルを取得した。独立側は`フィルタオブジェクト`、`色調補正`、内部側は`図形`、`標準描画`、`色調補正`の順で、両方式の色調補正は項目名、順序、値形式、トラック形式、有効・ロック状態が同一だった。
- サンプルの現在値は独立側が明るさ`51.60`・コントラスト`88.30`、内部側が両方`100.00`だった。これはサンプルの設定差であり、初期値と利用項目は両方式で共通、独立配置では`フィルタオブジェクト`が所有本体になるだけであることをユーザー確認として記録した。
- 内部追加済み円をレイヤー2・1266～1666フレームへ複製した。独立構成は円をレイヤー2、フィルタオブジェクトを直下のレイヤー3・865～1265フレームへ複製した。3個体すべて全比較項目が一致し、`recreation_verification.verified=true`となった。
- フィルター学習規則を、独立生成では`フィルタオブジェクト`本体を付け、通常オブジェクト内部への追加では本体を除外して同じフィルターブロックを末尾へ追加する方式に確定した。フィルターブロックの値変換は行わず、両方式とも生成後の詳細比較を必須とする。
- 色調整関係の標準フィルター初期値として、`色調補正`、`グラデーション`、`単色化`、`特定色域変換`、`拡張色設定`を独立したフィルタオブジェクトから取得した。5件とも`フィルタオブジェクト`本体は有効・ロックあり、後段の対象フィルターは有効・ロックなしだった。
- 5件をレイヤー0からレイヤー1の同じフレーム範囲へ順番に再生成した。各401フレームの長さ、エフェクト順、全設定値、有効・ロック状態、中間点、トラック情報が一致し、5件すべて`recreation_verification.verified=true`となった。
- グラデーションの中心XYは2項目グループ、拡張色設定のRGBは3項目グループだった。特定色域変換の初期値では`変換前の色`、グラデーションでは`終了色`が空文字列として保持された。確認済みブロックと未確認範囲を`ALIAS_CATALOG.md`へ登録した。
- クリッピング関係の標準フィルター初期値として、`クリッピング`、`斜めクリッピング`、`扇クリッピング`、`マスク`を独立したフィルタオブジェクトから取得した。4件とも`フィルタオブジェクト`本体は有効・ロックあり、後段の対象フィルターは有効・ロックなしだった。
- 4件をレイヤー0からレイヤー1の同じフレーム範囲へ順番に再生成した。各401フレームの長さ、エフェクト順、全設定値、有効・ロック状態、中間点、トラック情報が一致し、4件すべて`recreation_verification.verified=true`となった。
- 斜めクリッピングと扇クリッピングの中心XY、マスクのXYは各2項目グループだった。ユーザー提供画像から、マスク種類には8図形、現在のシーン名、仮想バッファ、ファイル選択操作が並ぶことを確認し、固定値・プロジェクト依存値・選択操作を区別して`ALIAS_CATALOG.md`へ記録した。
- ぼかし関係の標準フィルター初期値として、`ぼかし`、`レンズブラー`、`放射ブラー`、`方向ブラー`、`モーションブラー`を独立したフィルタオブジェクトから取得した。5件とも`フィルタオブジェクト`本体は有効・ロックあり、後段の対象フィルターは有効・ロックなしだった。
- 5件をレイヤー0からレイヤー1の同じフレーム範囲へ順番に再生成した。各401フレームの長さ、エフェクト順、全設定値、有効・ロック状態、中間点、トラック情報が一致し、5件すべて`recreation_verification.verified=true`となった。
- レンズブラーの`形状`候補は標準図形と同じ8種類であることをユーザー確認として記録した。生成比較済みは初期値の`円`だけとし、放射ブラーの`X`と`Y`には項目グループがないことも含めて、確認済みブロックと未確認範囲を`ALIAS_CATALOG.md`へ登録した。
- 光効果関係の標準フィルター初期値として、`グロー`、`発光`、`閃光`、`拡散光`、`色ずれ`を独立したフィルタオブジェクトから取得した。5件とも`フィルタオブジェクト`本体は有効・ロックあり、後段の対象フィルターは有効・ロックなしだった。
- 5件をレイヤー0からレイヤー1の同じフレーム範囲へ順番に再生成した。各401フレームの長さ、エフェクト順、全設定値、有効・ロック状態、中間点、トラック情報が一致し、5件すべて`recreation_verification.verified=true`となった。グロー、発光、閃光の初期値では`光色`の空文字列も保持された。
- ユーザー提供画像から、グローの形状7候補、閃光の合成モード3候補、色ずれの種類6候補を記録した。生成比較済みはそれぞれ初期値の`クロス(4本)`、`前方に合成`、`赤緑A`だけとし、確認済みブロックと未確認範囲を`ALIAS_CATALOG.md`へ登録した。
- 抽出1件として`エッジ抽出`、変形4件として`波紋`、`ラスター`、`ディスプレイスメントマップ`、`メッシュ変形`の初期値を独立したフィルタオブジェクトから取得した。5件とも`フィルタオブジェクト`本体は有効・ロックあり、後段の対象フィルターは有効・ロックなしだった。
- エッジ抽出はレイヤー0からレイヤー2、変形4件はレイヤー1からレイヤー3の同じフレーム範囲へ再生成した。各401フレームの長さ、エフェクト順、全設定値、有効・ロック状態、中間点、トラック情報が一致し、5件すべて`recreation_verification.verified=true`となった。
- ユーザー提供画像から、ディスプレイスメントマップの変形方法3候補と、図形・シーン・仮想バッファ・ファイル選択を含むマップ種類候補を記録した。メッシュ変形では展開された詳細設定から`分割数=50`と空の`座標`を取得・再生成確認し、確認済みブロックと未確認範囲を`ALIAS_CATALOG.md`へ登録した。
- 加工関係の標準フィルター初期値として、`シャープ`、`ノイズ除去`、`インターレース解除`、`画像合成`、`ノイズ`、`モザイク`を独立したフィルタオブジェクトから取得した。6件とも`フィルタオブジェクト`本体は有効・ロックあり、後段の対象フィルターは有効・ロックなしだった。
- 6件をレイヤー0からレイヤー1の同じフレーム範囲へ順番に再生成した。各401フレームの長さ、エフェクト順、全設定値、有効・ロック状態、中間点、トラック情報が一致し、6件すべて`recreation_verification.verified=true`となった。画像合成では空の`画像`と有効な2つのループ設定も保持された。
- ユーザー提供画像から、インターレース解除方法3候補、画像合成モード5候補、ノイズ合成モード2候補、ノイズ種類6候補を記録した。生成比較済みはそれぞれ初期値の`奇数`、`色情報を上書き`、`アルファ値と乗算`、`Type1`だけとし、確認済みブロックと未確認範囲を`ALIAS_CATALOG.md`へ登録した。
- シーンチェンジ関係11種類を取得した。これらは`フィルタオブジェクト`を持たず、`シーンチェンジ`または各`〇〇(シーンチェンジ)`エフェクト1件だけを持つ401フレームの独立オブジェクトで、全件が有効・ロックなしだった。
- レイヤー0からレイヤー1の同区間へ順番に再生成し、`シーンチェンジ`、発光、レンズブラー、ドア、起き上がる、リール回転、図形ワイプ、砕け散る、ページめくりの9種類は全比較項目が一致して`recreation_verification.verified=true`となった。
- `図形で隠す`と`図形で隠す(放射)`は、元の`エッジ高さ=1.0`が再生成時に`0.3`へ変化し、2件とも`create_verification_failed`となった。生成物は自動削除され、成功扱いにせず`要補正`として記録した。
- 図形ワイプ、図形で隠す、図形で隠す(放射)の`図形名`候補は標準図形と同じ8種類であることをユーザー確認として記録した。生成比較済みは円だけとし、確認済みブロックと未確認範囲を`ALIAS_CATALOG.md`へ登録した。
- `図形で隠す`と`図形で隠す(放射)`のエイリアス生成直後に限り、`エッジ高さ`が元値から変化した場合はSDK設定書込みで元値へ戻し、再読取後に通常の全詳細比較を行う限定補正を実装した。補正書込みまたは検証に失敗した場合は生成物を削除する。
- 限定補正を含むWin64 Debug版をビルドしてAviUtl2を再起動した。再起動後の両元サンプルはユーザー提供画面および外部取得の双方で`エッジ高さ=0.3`となり、これが保存後の正規初期値であることを確認した。
- 正規値の2種類をレイヤー0からレイヤー1の同区間へ再生成し、全設定値、有効・ロック状態、中間点、トラック情報が一致して、両方とも`recreation_verification.verified=true`となった。カタログを正規値`0.3`と生成確認済みへ更新した。
- 基本効果として、独立配置した`ローテーション`と`反転`の初期値を取得した。両方とも`フィルタオブジェクト`本体は有効・ロックあり、後段の対象フィルターは有効・ロックなしで、最新ビルドの`object_type=filter_object`分類も実機確認できた。
- ローテーションをレイヤー0・0～800フレーム、反転をレイヤー0・801～1601フレームから、それぞれレイヤー1の同区間へ再生成した。各801フレームの全設定値、状態、中間点、トラック情報が一致し、両方とも`recreation_verification.verified=true`となった。
- 反転はユーザー提供画像と外部取得の双方で、上下・左右・輝度・色相・透明度の5フラグがすべて初期値`0`であることを確認し、確認済みブロックと未確認範囲を`ALIAS_CATALOG.md`へ登録した。
- アニメーション効果として、独立配置した`リール回転`と`粒子化`の初期値を取得した。両方とも`フィルタオブジェクト`本体は有効・ロックあり、後段の対象フィルターは有効・ロックなしだった。
- リール回転をレイヤー0・0～800フレーム、粒子化をレイヤー0・801～1601フレームから、それぞれレイヤー1の同区間へ再生成した。各801フレームの全設定値、状態、中間点、トラック情報が一致し、両方とも`recreation_verification.verified=true`となった。
- 通常フィルターの`リール回転`は`リール回転(シーンチェンジ)`と別エフェクトとして区別し、粒子化の11数値項目には移動モードと項目グループがないことを含めて、確認済みブロックと未確認範囲を`ALIAS_CATALOG.md`へ登録した。
- 標準音声フィルターとして、独立配置した`音量フェード`、`モノラル化`、`音量調整`の初期値を取得した。3件とも`フィルタオブジェクト`本体と後段の音声効果が有効・ロックありで、映像系の独立フィルターとは後段効果のロック状態が異なっていた。
- 音量フェードをレイヤー0・0～800フレーム、モノラル化を801～1601フレーム、音量調整を1602～2402フレームから、それぞれレイヤー1の同区間へ再生成した。各801フレームの全設定値、状態、中間点、トラック情報が一致し、3件すべて`recreation_verification.verified=true`となった。
- 各音声パラメーターは移動モードなしの単独トラックであることを確認し、確認済みブロックと未確認範囲を`ALIAS_CATALOG.md`へ登録した。
- ユーザー提供の2枚の候補一覧画像から、標準`シーンチェンジ.種類`の20候補を重複なく確認した。クロスフェード、暗転、ホワイトアウト、スライス、ランダムライン、スライド、押し出し、スワップ、縮小回転、回転、キューブ回転、放射ブラー、ぼかし、およびワイプ7種類を`ALIAS_CATALOG.md`へ登録した。
- レイヤー0・0～800フレームのクロスフェード初期値サンプルをレイヤー1の同区間へ再生成し、801フレームの全設定値、状態、中間点、トラック情報が一致して`recreation_verification.verified=true`となった。20候補のうち生成比較済みはクロスフェードだけとして区別した。
- ユーザー確認により、標準オブジェクト、カスタムオブジェクト、標準フィルターの初回学習対象がすべて完了した。次の学習対象を外部フィルタープラグインへ切り替えた。
- `preview_create_object_from_alias`と`create_object_from_alias`を追加した。最新状態、完全な1オブジェクト分のエイリアス、1 MiB制限、空き生成範囲を事前検証し、生成直後に配置、主効果、素材パス、効果順、全指定値を照合して、不一致時は生成物を削除する。
- 一時生成した640×360の基準PNGを参照し、`画像ファイル`、`標準描画`、`ぼかし`を含む画像オブジェクトをレイヤー0・1300～2100フレームへPipeだけで生成した。生成結果は素材パス、3効果の順序、全指定値が一致した。
- 1500フレームで`ぼかし.範囲=5`と`50`を比較した。Pipeによる設定変更は`verified=50`となり、キャプチャ比較では2,073,600画素中338,500画素、16.3243%が変化した。細線、市松模様、輪郭、文字、色境界の軟化が強くなることを確認し、[`EFFECT_LEARNING.md`](EFFECT_LEARNING.md)へ記録した。
- 同じ基準PNGを参照し、`画像ファイル`、`標準描画`、`色調補正`を含む画像オブジェクトをレイヤー0・2200～3000フレームへPipeだけで生成した。生成結果は素材パス、3効果の順序、中立値6項目が一致した。
- 2400フレームで色調補正の明るさ、コントラスト、色相、輝度、彩度を個別に変更し、Pipeで書込み値を検証して各状態をキャプチャした。明るさはRGB全体の倍率、コントラストは中間灰色への距離、色相は色回転、輝度は輝度成分、彩度0はグレースケール化として作用することを確認した。
- `飽和する`は明るさ120と200でオン・オフを比較したが、不透明RGB基準画像ではいずれも画素差0だった。作用条件を未確認として区別し、全パラメーターを中立値へ戻して[`EFFECT_LEARNING.md`](EFFECT_LEARNING.md)へ記録した。

## 2026-07-22

- AviUtl2 v2.10で`AI MIRAI`ウィンドウの起動と表示を確認した。
- read section内で全レイヤーをオブジェクト単位に探索し、実プロジェクトの4オブジェクトを31 msで取得した。
- PowerShell標準の`NamedPipeClientStream`から、専用クライアントEXEを使わず接続できることを確認した。
- `get_edit_state`、`get_scene_objects`、`get_objects_at_cursor`、`get_selected_objects`のJSON応答を確認した。
- `audio_test.aup2`でプロジェクトパス、シーン`Root`、カーソル534フレーム、全4オブジェクトを取得した。
- カーソル位置にあるグループ制御、音声、動画の3オブジェクトと、それぞれのエフェクト設定値を取得した。
- SDKの複数選択APIだけでは通常選択が0件になることを確認し、`get_focus_object`の対象と複数選択対象を統合する方式へ変更した。
- 統合後、通常選択されたレイヤー1の`グループ制御(音声)`を`selected: true`、`focused: true`として取得した。
- 同じ選択オブジェクトから2エフェクト、106設定項目を取得し、値の切り捨てがないことを確認した。
- 全成功応答へ`snapshot_id`、`state_token`、`captured_at_utc`を追加した。
- 同一状態を3回取得し、`state_token`が一致して`snapshot_id`が毎回変わることを確認した。
- カーソルを144から0へ変更した場合に`state_token`が変わることを確認した。
- 同じテストプロジェクトパスでカーソルなどを同一に保ち、画像ファイルの`表示番号`だけを0から1へ変更した場合にも`state_token`と`content_digest`が変わることを確認した。
- `preview_set_object_parameter`を追加し、選択画像の`表示番号`を0から1へ変更する予定を実変更なしで取得した。
- プレビュー後の再取得で実値0と`state_token`が維持され、`applied=false`であることを確認した。
- 同値0の指定では`will_change=false`、古いトークンでは`state_changed`、存在しない対象では`target_not_found`となることを確認した。
- `set_object_parameter`を追加し、`state_token`、選択状態、対象範囲、内容ハッシュ、変更前値を編集直前にも再検証するようにした。
- 実変更にはJSON Booleanの`apply: true`を必須とし、`apply=false`が`apply_required`で拒否されることを確認した。
- テスト画像の`表示番号`を0から1へ変更し、`applied=true`、`verified=1`、状態指紋の変化、再取得値1を確認した。
- 更新後の`state_token`を使って`表示番号`を1から0へ戻し、`verified=0`、再取得値0を確認した。
- 同値指定では書込みを省略して`applied=false`、`changed=false`となり、古いトークンでは`state_changed`となることを確認した。
- 書込みはAviUtl2 SDKの`call_edit_section_param`内で行い、1要求を1つのUndo単位として登録する構成とした。
- `preview_set_object_parameters`と`set_object_parameters`を追加し、最大64件を全件事前検証して1回の編集コールバックで変更できるようにした。
- 一括変更内の同一項目重複を`duplicate_change`として拒否し、書込み途中の失敗時には適用済み項目を同じコールバック内で戻す構成とした。
- テスト画像の`表示番号`を0から1、`標準描画.X`を0.00から10.00へ一括変更し、`changed_count=2`、検証値、状態指紋の変化、再取得値を確認した。
- 更新後トークンを使った一括変更で両項目を元へ戻し、再取得で`表示番号=0`、`X=0.00`を確認した。
- `apply=false`、重複項目、2件目の存在しない項目が全書込み前に拒否され、同値2件では書込みを省略することを確認した。
- `preview_move_objects`と`move_objects`を追加し、最大64個の選択オブジェクトを全件事前検証して1回の編集コールバックで移動できるようにした。
- テスト画像をレイヤー0・144～224フレームからレイヤー1・300～380フレームへ移動し、状態指紋の変化と再取得位置を確認した。その後、同じコマンドで元位置へ戻した。
- 同位置指定では`applied=false`、`moved_count=0`となり、`apply=false`、重複対象、古いトークンが拒否されることを確認した。
- `audio_test.aup2`で、選択中のグループ制御をレイヤー2・0フレームへ移動するプレビューが既存音声と重なり、`destination_occupied`で拒否されることと、対象位置および状態指紋が不変であることを確認した。
- 一括移動の途中失敗時は、同じ編集コールバック内で適用済み対象を元位置へ戻す構成とした。
- `preview_duplicate_objects`と`duplicate_objects`を追加し、最大64件の選択オブジェクトを空き位置へ1回の編集コールバックで複製できるようにした。
- テスト画像をレイヤー1・300～380フレームへ複製し、`created_index=1`、全2オブジェクト、種類、エフェクト構成、全設定値の一致を確認した。
- 同じ選択画像を1要求でレイヤー1・300～380フレームとレイヤー2・400～480フレームへ複製し、`created_index=1,2`と全3オブジェクトを確認した。
- AviUtl2が生成時にエイリアス内部を正規化して複製元と生成物の`content_digest`が変わることを確認し、生成検証を配置、表示名、種類、エフェクト順、全設定項目名・値の照合へ変更した。
- `apply=false`、未選択の複製元、古いトークン、既存オブジェクトとの重なり、同一要求内の生成先重複が拒否されることを確認した。
- 複製を行ったテストプロジェクトを保存せず終了し、再度開いた際に元の1オブジェクトだけであることを確認した。
- 一括複製の途中失敗時は、同じ編集コールバック内で生成済みオブジェクトを削除する構成とした。
- AviUtl2 v2.10 SDKを照合し、現在シーンID・名前と変更通知はあるが、シーン一覧列挙、切り替え、追加APIが存在しないことを確認した。非公開APIは使用せず未対応とした。
- `preview_set_edit_position`と`set_edit_position`を追加し、カーソル位置と選択範囲を個別または同時に設定できるようにした。
- テストプロジェクトでカーソルを144から100フレームへ移動し、選択範囲50～120を設定して再取得した。その後、カーソル144と選択解除へ戻した。
- `selection: null`による解除、同値指定の`applied=false`、範囲外レイヤー・フレーム範囲、`apply=false`、古いトークンの拒否を確認した。
- カーソルと選択範囲はプロジェクト編集のUndo対象ではないUI状態として扱うことを仕様化した。
- AviUtl2 v2.10公開SDKに既存オブジェクトの終了フレーム・長さを直接変更するAPIがないことを確認した。削除と再生成による代替は連結、中間点、外部プラグイン固有情報を損なう可能性があるため採用しなかった。
- `get_scene_objects`系の応答へ、レイヤー名と有効・ロック状態、オブジェクトの区間開始フレームとフォーカス区間、各エフェクトの有効・ロック状態を追加した。これらも`state_token`の計算対象とした。
- AviUtl2 v2.1.0のテスト画像で、レイヤー0の有効・ロック状態、区間数1・開始フレーム144・フォーカス区間0、`画像ファイル`と`標準描画`の有効・ロック状態を取得した。
- AviUtl2 v2.1.0の`audio_test.aup2`で4レイヤー、4オブジェクトの区間情報と、標準エフェクトおよび外部エフェクト`Aul2Audio View`の有効・ロック状態を取得した。
- AviUtl2 beta50では後発のエフェクト状態APIが利用できないことを確認し、要求全体を失敗させず`effect_states=[]`、`effect_details[].state_available=false`として返す互換処理を確認した。
- `get_objects_in_selection`を追加し、現在の選択範囲と1フレーム以上重なるオブジェクトを全レイヤーから詳細情報付きで取得できるようにした。
- AviUtl2 v2.1.0の`audio_test.aup2`で選択範囲0～49に重なる3オブジェクトを取得し、50フレーム開始のカメラ制御が除外される境界判定を確認した。選択範囲の解除後は`selection_range_not_set`で拒否されることも確認した。
- `get_object_details`を追加し、最新の`state_token`と応答内`index`を指定して対象1件だけの設定詳細を取得できるようにした。
- `audio_test.aup2`のindex 0を指定し、カメラ制御1件と1エフェクトの詳細を取得した。存在しないindex、負のindex、古い状態をそれぞれ`target_not_found`、`invalid_target_index`、`state_changed`で拒否することを確認した。
- 範囲取得時の詳細展開条件を補強し、選択範囲0～49の3オブジェクトすべてで2件、2件、3件のエフェクト詳細を取得した。
- 複数オブジェクトのグループ関係を調査し、公開SDKに列挙APIがなく、`serif_test.aup2`の`group=1/2`が単一オブジェクトの`get_object_alias`には含まれないことを実機確認した。プロジェクトファイル直接解析や配置からの推測は採用せず未対応とした。
- 設定項目へ`track_info`を追加し、トラックバーの移動モード、内部パラメーター、加速・減速、中間点無視、時間制御、エフェクト内項目グループを取得できるようにした。
- AviUtl2 v2.1.0の`audio_test.aup2`で、カーソル上3オブジェクトの158設定項目中106項目をトラックバーとして取得した。音声・動画の`再生位置`で`再生範囲`モード、映像再生のXYZと回転XYZでそれぞれ3項目のグループ名・数・indexを確認した。
- AviUtl2 beta50でも同じ158設定項目中106項目のトラックバー情報を取得し、要求が正常終了することを確認した。
- `get_current_frame_image`を追加し、現在のカーソルフレームをAviUtl2のレンダリングAPIから32bit BMPへ保存できるようにした。
- AviUtl2 v2.1.0の空プロジェクトでフレーム0を1920×1080、8,294,454 bytesのBMPとして63 msで取得し、画像ファイルを正常に表示できることを確認した。
- `audio_test.aup2`のフレーム534を31 msで取得し、画像の上下方向とRGB色が正常であること、および画像取得後も`state_token`が変化しないことを確認した。
- `Setup\make_release_zip.bat`と`make_release_zip.ps1`を追加し、Release配置済みプラグインから`Setup\Aul2MIRAI.zip`を生成できるようにした。
- ZIP内の`Aul2MIRAI\Aul2MIRAI.aux2`とRelease配置物のSHA-256が一致することを確認した。将来の配布物はスクリプトの`packageFiles`へ追加する構成とした。
- AI MIRAI画面を本番向けに整理し、デバッグ用のRefresh、オブジェクト全文、操作ログを廃止して、製品説明、外部インターフェース状態、接続先、最終処理を表示する構成へ変更した。
- ヘルプボタンから既定ブラウザで`https://github.com/vramwiz/Aul2MIRAI`を開けることをAviUtl2 v2.1.0上で確認した。
- AI MIRAI画面をさらに簡素化し、最新の外部操作ログ1行、`待機中`・`完了`・`拒否`・`エラー`の短い状態、ヘルプボタンだけを表示する構成とした。`get_edit_state`後に1行ログと`状態: 完了`へ更新されることを実機確認した。
- 配布ZIPへ`README.md`、`AI_USAGE.md`、`PIPE_INTERFACE.md`を追加し、プラグインを含む4ファイルすべてが元ファイルとSHA-256一致することを確認した。
