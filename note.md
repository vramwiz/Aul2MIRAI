# Aul2MIRAI note

作業再開時に最初に見る開発メモ。プロジェクトの方針、判断、優先順位、未解決課題を中心に置く。個別インターフェースの詳細仕様は分離した文書へ記載する。

- 利用者向けの概要、配置、配布説明は `README.md` に置く。
- AIがAul2MIRAIの外部操作を学ぶための接続方法、コマンド仕様、注意事項、操作例は [`AI_USAGE.md`](AI_USAGE.md) に置く。
- サンプルから学習したオブジェクト・フィルターの生成用エイリアス、設定項目、値形式は [`ALIAS_CATALOG.md`](ALIAS_CATALOG.md) に置く。
- フィルターの設定値と実際の映像変化の対応は[`EFFECT_LEARNING.md`](EFFECT_LEARNING.md)に置く。
- AviUtl2付属`lua.txt`の要点、スクリプト制御の安全規則、実機で確認したLuaの挙動は[`LUA_SCRIPTING.md`](LUA_SCRIPTING.md)に置く。
- 全シーンの検出、実長算出、Rootへの一括配置手順は[`SCENE_LEARNING.md`](SCENE_LEARNING.md)に置く。
- Named Pipeの通信仕様、内部処理、接続情報、制限は [`PIPE_INTERFACE.md`](PIPE_INTERFACE.md) に置く。
- AI向けの操作手順は`AI_USAGE.md`、生成知識は`ALIAS_CATALOG.md`へ記録し、本ファイルへ個別エイリアスを追加しない。
- 完了済みの実装と実機確認結果は[`HISTORY.md`](HISTORY.md)に記録し、本ファイルには現在の判断に必要な要約だけを残す。
- 実装途中では文書を逐次更新せず、ビルド、AviUtl2上の表示、実機確認まで完了してから現状を反映する。

## 作業再開チェックポイント（2026-07-24）

### 現在地

- 標準オブジェクトとカスタムオブジェクトの初期値取得・空き位置への再生成比較は一巡完了した。確認済みの全ブロックは[`ALIAS_CATALOG.md`](ALIAS_CATALOG.md)、時系列は[`HISTORY.md`](HISTORY.md)にある。
- ユーザー確認により、標準オブジェクト、カスタムオブジェクト、標準フィルターの初回学習対象はすべて完了した。次の作業は外部フィルタープラグインの学習である。
- 次段階として実践的な効果学習を開始した。決定論的な基準画像へフィルターを適用し、パラメーターを段階変更してキャプチャと画素差を[`EFFECT_LEARNING.md`](EFFECT_LEARNING.md)へ記録する。
- フィルターは、独立した`フィルタオブジェクト`として配置した状態から学習してよい。通常オブジェクト内部にある場合と、フィルターブロックの初期値、利用項目、項目順、値形式、トラック形式は共通である。
- 独立配置と通常オブジェクト内部の違いは、フィルターブロックを所有する本体だけである。独立配置では`フィルタオブジェクト`、内部追加では元の通常オブジェクトが所有本体になる。
- 色調補正で両構成を比較し、項目体系が同一であることを実機確認した。独立構成2個体と内部追加済み1個体をそれぞれ複製し、3件すべて`recreation_verification.verified=true`を確認した。

### 現在の優先課題: シーン一括配置

- ユーザーが「シーンを全部配置して」と指示したとき、Rootへ存在する全シーンを順番に置き、各オブジェクトを参照先シーンの実長に合わせ、隙間なく並べることを目標とする。
- AviUtl2 v2.1.0公開SDKには全シーン列挙と各シーン長の取得APIがない。保存済み`.aup2`を読み取り専用で解析し、`[scene.N]`から一覧を、各所属オブジェクトの最大終了フレーム`+1`から実長を得る。
- `effect: "シーン"`、`length: 0`による標準初期値生成は可能になったが、参照先未設定で4001フレームとなる。生成後に`シーン`番号を変更しても長さと`再生位置`は追従しないため、一覧・長さ取得の正本には使わない。
- `preview_create_object_from_alias`と`create_object_from_alias`は、`length: 0`のSDK自動長と、`alias`の代わりに`effect`を指定する標準初期値生成へ対応した。
- 複数シーン配置用に`preview_create_objects_from_aliases`と`create_objects_from_aliases`を追加した。最大64件を先に一括検証し、1回の編集コールバックとUndoで生成する。生成途中の選択・フォーカス移動による`state_token`不一致を避け、途中失敗時は同バッチの生成物を全削除する。
- 再起動後の実機でScene1～Scene3を1要求から一括配置し、プレビュー3件、生成3件、配置後の詳細再取得がすべて成功した。途中の`state_token`不一致は発生しなかった。
- `seen_test.aup2`でScene1=5607、Scene2=3204、Scene3=603フレームと算出し、Rootのレイヤー0へ0～5606、5607～8810、8811～9413として連続配置した。全件のシーン番号、`再生位置`、効果順を再取得し、総長9414フレームを確認した。
- 現在の正しい配置は残す。再開時は勝手に削除・再生成せず、ユーザー指示と現在状態を確認する。
- 詳細な解析規則、完全エイリアス、配置手順、未確認事項は[`SCENE_LEARNING.md`](SCENE_LEARNING.md)を正本とする。

### 前段の優先課題: スクリプト制御

- ユーザー指示により、標準フィルターの実践的な効果学習は`レンズブラー`完了地点でいったん中断した。次に予定していた`放射ブラー`以降は、スクリプト制御の学習後に再開する。
- `スクリプト制御`は標準フィルターとして未学習だったため、AIがLuaを書いてオブジェクトへ動きや加工を与える用途を独立した学習テーマとする。
- 配置済みサンプルは、レイヤー0・9012～9212フレームの白い円で、エフェクト順は`図形`、`標準描画`、`スクリプト制御`である。Luaは`obj.time`を使う半径200画素・2秒周期の円運動である。
- 同じLuaを含む完全エイリアスをレイヤー0・9213～9413フレームへPipeだけで生成した。配置、主効果、素材パス、効果順、全項目名と値が一致し、相対時刻0、0.5、1.0、2.0秒のBMPも元サンプルと全組SHA-256一致した。
- 確認済みブロックでは、`スクリプト制御`は通常オブジェクトの出力エフェクト後に置き、設定項目は`スクリプト`1件だけである。Lua本文の改行はエイリアス上でリテラルの`\n`として1行に保存する。完全ブロックは[`ALIAS_CATALOG.md`](ALIAS_CATALOG.md)を参照する。
- AviUtl2 v2.1.0付属の`D:\aviutl2_v2.1.0\lua.txt`を読み、利用可能なライブラリ、変数、設定定義、関数群、安全上の制約を[`LUA_SCRIPTING.md`](LUA_SCRIPTING.md)へ整理した。
- レイヤー0・9213～9413の変更前の円運動を相対時刻0、0.5、1.0、1.5、2.0秒でPipeキャプチャした。白円の中心は上、右、下、左、上の理論位置と約0.5画素以内で一致し、2秒周期、半径200画素、サイズ不変を確認した。
- 同オブジェクトのLuaを、X振幅240画素、Y振幅120画素、4秒周期の横長の8の字運動へ変更した。`preview_set_object_parameters`後に適用し、0.5秒刻みの9点すべてで理論位置と0.04画素以内の一致を確認した。
- Lua本文を実改行で渡した最初の適用は`write_verification_failed`となり、元値が自動復旧された。リテラルの`\n`で行を連結すると適用と再読取が完全一致した。Pipeで`スクリプト`本文を変更するときはリテラル`\n`形式を使用する。
- ここまでで「既存Luaの読取」「同一Luaを含む完全エイリアスの新規配置」「公式Lua説明との照合」「円運動の理論値と表示の一致」「AI記述Luaへの安全な本文変更」「8の字運動の理論値と表示の一致」は確認済みである。

次回のスクリプト制御学習順:

1. AI記述Luaを持つ別の図形オブジェクトを、完全エイリアスとして空き区間へ新規生成する。
2. 拡大率と透明度を一種類ずつ試す。
3. 構文エラーと実行時エラーを意図的に作り、Pipe応答、AviUtl2の表示、復旧方法を確認する。エラー中の生成物を成果物として残さない。
4. 数式、文字列長、無限ループ、ファイル・外部アクセスなどの安全境界を確認し、AIが生成してよいLuaの制限を決める。
5. 通常トラック移動との使い分け、フィルターより前後へ置いた場合の処理順、他フィルターとの併用を確認する。
6. 確認済みのLuaパターン、配置ブロック、安全規則を[`ALIAS_CATALOG.md`](ALIAS_CATALOG.md)と[`EFFECT_LEARNING.md`](EFFECT_LEARNING.md)へ記録する。
7. スクリプト制御の基礎学習完了後、移動する円を使って`放射ブラー`から標準フィルターの実践学習を再開する。

スクリプト制御の実験もAviUtl2操作はNamed Pipeだけで行う。既存サンプルおよび生成済み確認物は残してよい。コード修正は必要なPipe命令が不足している場合だけ行う。

### フィルター学習・生成の確定規則

1. ユーザーが独立したフィルタオブジェクトとして次のフィルターサンプルを配置する。
2. `get_scene_objects`と`get_object_details`で、`フィルタオブジェクト`の後ろにあるフィルターブロックの全項目、値、状態、トラック情報を取得する。
3. 空きレイヤーへ独立フィルターを再生成し、詳細比較で`recreation_verification.verified=true`を確認する。
4. 独立フィルターとして生成するときは、空の`[0.0] フィルタオブジェクト`を付け、その後ろへ学習したフィルターブロックを置く。
5. 通常オブジェクト内部へ追加するときは、`フィルタオブジェクト`本体を除外し、学習したフィルターブロックだけを対象エイリアスの末尾へ追加する。項目名や値の変換は行わない。
6. 内部追加には、対象取得、削除、フィルターブロック追加済みエイリアスの同位置再生成、詳細比較、失敗時の元オブジェクト復元を行う`replace_source`方式を使用する。
7. 対象を削除しない追加が適する場合は、対象直下へ独立フィルタオブジェクトを生成する。公開SDKから作用対象の関連情報は取得できないため、レイヤー差、時間範囲、複数候補の配置規則を確認してから一般機能化する。
8. 両方式とも生成後の詳細比較を必須とする。AviUtl2や外部プラグインにより値が正規化された場合は成功扱いにせず、`要補正`として実測値を記録する。
9. 学習結果は[`ALIAS_CATALOG.md`](ALIAS_CATALOG.md)、完了した検証は[`HISTORY.md`](HISTORY.md)へ追記する。

### 当面継続する実践的な効果学習

標準フィルターは、初期値と生成形式を知るだけでなく、次の一連の処理をフィルターごとに繰り返して映像上の作用を学習する。ユーザーから変更指示があるまで、この方式を既定の学習方法とする。

1. 色帯、グラデーション、市松模様、細線、輪郭、文字を含む決定論的な一時画像を作成または使い回す。
2. 画像ファイル、標準描画、今回試すフィルターを1個の完全なオブジェクトエイリアスへ含め、空き時間へ生成する。
3. 生成直後に素材パス、配置、エフェクト順、全指定値を再取得して一致を確認する。
4. 中立状態または比較基準を`get_current_frame_image`で取得する。
5. 1つの意味を持つパラメーターだけを段階的に変更する。関連する複数項目は一括プレビュー後、1回のUndo単位で適用する。
6. 各状態をキャプチャし、目視、変化画素率、RGB成分の平均絶対差、最大差、変化領域を比較する。
7. 選択値、チェック項目、合成モードなどは候補を実際に切り替え、差が出ない場合も試した素材・値・条件を残す。
8. 比較後は次の実験を妨げない中立値へ戻す。
9. 確認できた作用と未確認条件を[`EFFECT_LEARNING.md`](EFFECT_LEARNING.md)、実施履歴を[`HISTORY.md`](HISTORY.md)へ記録する。

放射ブラー以降の時間方向の変化が必要なフィルターでは、静止画像だけで判断しない。白い円を動かす基準オブジェクトを使い、開始、途中、周期境界など複数フレームをPipeキャプチャして比較する。AIが検証用の決定論的な動きを新規生成する場合は、原則として`スクリプト制御`のLua式を使う。1ブロックのまま軌道、速度、周期を明示でき、中間点と区間ごとのトラック値を組み立てる必要がないためである。単純な直線移動、利用者がUIで調整する動き、Luaへ依存させたくない成果物では通常のトラック移動を使う。両方式の実測構成は[`EFFECT_LEARNING.md`](EFFECT_LEARNING.md)の「動き比較用基準オブジェクト」を参照する。

AviUtl2の操作はNamed Pipe命令だけで行い、マウスカーソル、キーボード操作、画面座標による自動操作は使わない。必要なPipe命令が存在しない場合だけ、先に不足機能を実装・ビルド・実機確認してから実験を再開する。

### 現在の実機サンプル

起動中のAviUtl2には、独立配置と内部追加を比較する次の6個体が残っている。

| 構成 | レイヤー | フレーム | 内容 |
| --- | ---: | --- | --- |
| 独立構成・元 | 0 | 865～1265 | 赤い円、`色=ff0000`、内部フィルターなし |
| 独立構成・元 | 1 | 865～1265 | `フィルタオブジェクト`＋色調補正、明るさ`51.60`、コントラスト`88.30` |
| 内部追加・元 | 0 | 1266～1666 | 赤い円＋色調補正、明るさ`100.00`、コントラスト`100.00` |
| 独立構成・複製 | 2 | 865～1265 | 赤い円の複製、詳細比較一致 |
| 独立構成・複製 | 3 | 865～1265 | 独立フィルターの複製、詳細比較一致 |
| 内部追加・複製 | 2 | 1266～1666 | 内部追加済み円の複製、詳細比較一致 |

- オブジェクトの`index`と`state_token`は一時値なので再開時に保存値を使用しない。必ず`get_scene_objects`で最新状態を取得し、レイヤー、フレーム、`primary_effect`、エフェクト順から対象を選び直す。
- 現在起動中のプラグインは、ソースへ追加済みの最新分類が未反映である可能性がある。実機応答では`フィルタオブジェクト`が`object_type=unknown`でも、`primary_effect=フィルタオブジェクト`で識別できる。最新ソースは`filter_object`および`screen_effect`分類を含み、Win64 Debugの確認用ビルドに成功している。
- 再開時に分類名の実機確認が必要なら、ユーザーが最新ソースを通常ビルドしてAviUtl2を再起動した後、一覧を再取得する。フィルター学習自体は`primary_effect`で識別できるため、分類名未反映でも継続できる。

### 再開時に最初に行うこと

1. 本節と[`SCENE_LEARNING.md`](SCENE_LEARNING.md)を読む。
2. 起動中のAviUtl2へ`get_edit_state`と`get_scene_objects`を実行し、現在シーンがRootであることと、Scene1～Scene3の配置0～9413が残っているか確認する。`index`は保存値を使わない。
3. シーン配置にはユーザーが用意した実験状態を使い、削除・再生成は明示指示がある場合だけ行う。
4. 空シーン、未保存変更、欠番、表示順などの未確認事項をユーザーの準備に合わせて学習する。
5. シーン学習の区切り後、スクリプト制御の残課題、続いて標準フィルターの`放射ブラー`へ戻る。

## 現在の方針

- プロジェクト名は `Aul2MIRAI`、AviUtl2上の表示名は `AI MIRAI` とする。
- `D:\DelphiProg\test\Aul2MIRAI` で、Delphi Win64のAviUtl2拡張プラグインとして開発する。
- `Aul2AudioController` の拡張プラグイン境界を参考にするが、必要になるまで機能や依存ユニットを増やさない。
- AviUtl2 SDKの原本は `C:\Users\vramw\Downloads\aviutl2_sdk_v210\plugin2.h` とし、プロジェクト内の `Source\Lib\AviUtl2Plugin\plugin2.h` にコピーして参照する。
- Delphi実装の参照元は `D:\DelphiProg\test\Aul2AudioFilter` とする。ただしSDK仕様は公式 `plugin2.h` を優先する。
- AviUtl2 SDK型定義など、共通利用できるコードは `Source\Lib` に置く。
- AviUtl2へ公開する入口とUI・機能実装を分け、`.dpr` を肥大化させない。
- SDKコールバックは `cdecl` を維持する。Delphi例外をAviUtl2側へ漏らさない。
- 初期化解除では、AviUtl2やSDKへの参照が無効になる前にウィンドウやGDIリソースを解放する。

## AviUtl2外部操作インターフェース構想（検討中）

### 基本方針

AviUtl2の編集作業にAIを活用する方法として、AIを内蔵した専用プラグインではなく、AviUtl2の情報取得や編集操作を外部へ公開するインターフェースを提供する構成を検討する。

CodexなどのAIは、アプリに付属する、またはWebサイトから配布するAI向け仕様書を作業開始時に読み、そのセッション内でインターフェースの使い方を理解してAviUtl2を操作する。AIモデルやAPIをアプリへ直接組み込まず、特定のAIサービスや契約形態へ依存しない。仕様を公開し、Codex以外の外部ツールからも利用できる構成を目指す。

Aul2MIRAIと外部アプリの位置付けは「AI編集アプリ」ではなく、AIからも利用できるAviUtl2外部操作ブリッジとする。何を行うかはAIとユーザーが判断し、ブリッジは指定された情報の取得や操作を正確に実行する。

読み取り専用インターフェースの初期実装と実機検証を進めている。編集操作は読み取り形式と安全規則が安定するまで実装しない。

### 基本構成

| 要素 | 役割 |
| --- | --- |
| AviUtl2用プラグイン | AviUtl2内部の情報取得と編集操作を担当する。 |
| 外部接続手段 | 専用EXEを使わず、OS標準機能からプラグインへ接続する。詳細は[`PIPE_INTERFACE.md`](PIPE_INTERFACE.md)に置く。 |
| [`AI_USAGE.md`](AI_USAGE.md) | 接続方法、コマンド仕様、注意事項、操作例をAIへ伝える。 |

### 外部接続方針

- 専用クライアントEXEは作成・配布・使用しない。
- Windowsの警告対象になりやすい未署名EXEと、クライアントバイナリの配布・更新を不要にする。
- 通信方式の実装詳細は[`PIPE_INTERFACE.md`](PIPE_INTERFACE.md)へ集約し、本ファイルへ重複して記載しない。

具体的な操作手順は[`AI_USAGE.md`](AI_USAGE.md)へ記載し、Codexは作業開始時にそれを読んで実行する。

初期版は読み取り専用とし、公開範囲を小さく保つ。編集操作は読み取り形式と安全規則が安定してから検討する。

開発用の本ファイル `note.md` と、将来アプリへ同梱・公開するAI向け仕様書 `AI_USAGE.md` は役割を分ける。`AI_USAGE.md` をアプリに同梱すれば、そのバージョンに対応した説明を確実に提供できる。Webサイトからも取得できるようにすれば、アプリ本体を更新せずに説明や操作例を改善できる。

AI向け仕様書はAIへ恒久的に学習させるものではない。作業開始時に読み込ませ、そのセッションで操作方法を理解させる方式とする。

### AI向け仕様書に記載する内容

- 接続方法とコマンド仕様
- 取得できる情報とデータ形式
- AviUtl2固有の用語
- 操作時の基本規則
- 標準オブジェクトの扱い方
- 具体的な使用例
- 対応するプラグイン、外部アプリ、インターフェース仕様のバージョン

### 安全性

- 安全上の判断は原則としてCodexなど呼び出し側の規約に従う。
- 存在しないオブジェクト識別子、範囲外の数値、不正なコマンドなど、機械的に判定できる問題はアプリ側でも拒否する。
- 状態変更後の古い取得結果を使った操作を防ぐため、プロジェクト、シーン、オブジェクト、取得時点を識別できる情報を返す方式を検討する。
- 削除や大規模な自動変更は初期段階では扱わない。
- 変更系を導入するときは、変更内容の事前確認と、複数変更を一回のUndoで戻せる仕組みを検討する。
- 読み取り機能を基礎とし、変更は選択中オブジェクトの単一設定値から段階的に検証する。

### AI活用の方向性

セリフの整列、オブジェクトの移動、字幕の長さ調整などの定型操作は、専用機能や手動操作の方が速い場合が多く、AIを介在させる利点が小さい。長時間の動画解析や文字起こしも中心機能にしない。AviUtl2内に元のセリフやオブジェクト情報が存在する場合は、完成動画全体を解析するより編集アプリ上の情報を直接利用する方が効率的である。

AIを使う意味がある候補として、既存シーンの構造と各オブジェクトの役割を判断し、新しいシーンの骨組みを作る機能がある。

例として「このシーンを基に、セリフ以外をコピーして次のシーンを作る」という指示では、次のような判断をAI側で行う。

- 背景や画面装飾はコピーする。
- 固定表示される立ち絵はコピーする。
- セリフ音声や字幕はコピーしない。
- 口パクやセリフ固有の表情差分はコピーしない。
- BGMは複製せず、そのまま継続させる。
- 画面効果や共通フィルターは必要に応じて引き継ぐ。

単純に指定レイヤーを複製するのではなく、オブジェクトの種類、名称、素材パス、配置、関連関係などから役割を判断できる点をAI利用の利点とする。ただし、この変更機能は将来段階とし、最初は情報取得から着手する。

### 第1段階：基本状態の取得

まず、AviUtl2が現在どのような編集状態にあるかを外部から取得する。

主な取得候補:

- AviUtl2の起動状態
- プロジェクト名と識別情報
- 動画サイズ
- フレームレート
- シーン一覧
- 現在編集中のシーン
- シーンの長さ
- 現在のカーソル位置
- 選択範囲
- 選択中のオブジェクト

時間位置は、正確な処理に使うフレーム番号と、人が理解しやすい秒数の両方を返すことを想定する。

### 第2段階：シーン上のオブジェクト取得

基本状態を取得できたら、現在のシーン上にあるオブジェクト情報を取得する。

取得単位の候補:

- 現在のシーンにある全オブジェクト
- カーソル位置に存在するオブジェクト
- 選択範囲と重なるオブジェクト
- 選択中のオブジェクト
- 識別子を指定した単一オブジェクト

オブジェクトの共通情報:

- オブジェクト識別子
- オブジェクトの種類
- 表示名
- 開始・終了フレーム
- 配置レイヤー
- 選択状態
- 有効・無効状態
- グループや連結関係
- 素材ファイルのパス
- 適用されているフィルター
- 中間点の有無
- 標準パラメーター

最初は次のAviUtl2標準オブジェクトを対象とする。

- 動画
- 画像
- 音声
- テキスト
- 図形
- シーン
- フレームバッファ
- グループ制御
- カメラ制御
- 標準フィルター効果

外部プラグインの独自オブジェクトや独自フィルターについて、ブリッジ本体が個別に内部構造を解析することは想定しない。取得可能であれば、名称、配置位置、レイヤー、提供元などの共通情報だけを返す。

詳細な対応が必要な場合は、各プラグインの作者やユーザーが追加仕様を用意する。インターフェースが必要な情報を公開できていれば、別途用意した説明書をAIへ読ませることで利用範囲を拡張できる。

### 最初の到達目標

初期試作は、Syncroh2の次オブジェクト探索方式を参考に、現在のシーンにあるオブジェクト情報を全レイヤーから取得し、AI MIRAI画面と読み取り専用外部インターフェースで確認できるところまでとする。編集操作はこの段階へ含めない。

実装方針:

- 公式v2.10 SDKの読み取り用 `call_read_section` を使用する。
- 外部からの取得要求を実装する前に、プラグイン画面の手動更新操作で取得経路を検証する。
- 基本編集情報は `get_edit_info` で先にコピーする。公式SDKの仕様上、read section内の `EDIT_SECTION.info` は利用できない。
- その後、1回の読み取りコールバック内で `0` から `layer_max` までの全レイヤーを探索する。
- 各レイヤーでは `find_object` が返したオブジェクトの終了フレーム `+1` へ移動し、フレーム単位の総当たりを行わない。
- SDKコールバック内では必要な値を通常のDelphiデータへコピーし、SDKの一時ポインターやオブジェクトハンドルを保持しない。
- 最初の一覧では、種類、表示名、レイヤー、開始・終了フレームなどの共通情報だけを取得する。
- エイリアス全文、全フィルター、全設定項目などの詳細取得は別段階とする。
- 不正なフレーム範囲、進行しない探索、過大な件数に対する停止条件を設ける。
- 取得処理、スナップショット型、表示、操作ログ、プラグイン登録を用途別の小さいユニットへ分ける。
- 1ユニットへSDKアクセス、データ整形、描画、通信処理を混在させない。
- 外部通信のスレッド構成と終了順序は[`PIPE_INTERFACE.md`](PIPE_INTERFACE.md)に従う。

初期試作では次の流れを成立させる。

1. 現在のプロジェクト、シーン、カーソル位置を取得する。
2. カーソル位置に存在するオブジェクトを取得する。
3. 選択オブジェクトの詳細情報を取得する。
4. 標準オブジェクトを種類別に識別する。
5. 未対応オブジェクトは取得可能な共通情報だけを返す。
6. Codexが取得結果を読み、現在の編集状態を説明する。

現在は、手動Refreshと外部要求による全レイヤーのオブジェクト取得、一覧表示、状態表示、操作ログまで実装済み。編集状態、全オブジェクト、カーソル位置のオブジェクト、現在の選択範囲と重なるオブジェクト、通常選択と複数選択を統合した選択オブジェクトを取得できる。最新の`state_token`と応答内`index`を組み合わせ、単一オブジェクトだけの詳細を取得することもできる。標準オブジェクトの種類、素材パス、適用エフェクト名に加え、レイヤー名と有効・ロック状態、オブジェクトの区間開始フレームとフォーカス区間、各エフェクトの有効・ロック状態も返す。絞り込み取得では、エフェクトごとの設定項目名・値と、トラックバーの移動モード、内部パラメーター、補間フラグ、項目グループも返す。現在のカーソルフレームはAviUtl2でレンダリングし、一意な一時BMPとして取得できる。各成功応答には要求単位の`snapshot_id`と、編集状態の変更を検出する`state_token`を付ける。選択オブジェクトの設定値は、事前プレビュー、最新状態の一致、明示的な`apply: true`、書込み後の再取得検証を条件に変更できる。関連する最大64件を全件事前検証し、1回のUndo単位で一括変更する機能も実装済み。選択オブジェクトの移動と複製も最大64件を一括指定できる。複製ではエイリアス再生成直後に、配置以外の設定、中間点、エフェクト状態、トラック情報を元と詳細比較し、不一致なら生成物を削除する。標準画像では一致したが、外部フィルターでは設定値が変わる実例を確認したため、将来のフィルター追加でも再生成後の詳細比較を必須とする。安全のため移動・生成先が他オブジェクトの現在範囲と重なる要求や位置交換は拒否する。カーソル位置と選択範囲はプレビュー後に設定・解除できる。外部向けの一般削除は行わず、フィルター追加時の内部置換と失敗時のロールバックに限って削除を使用する。詳細な確認結果は[`HISTORY.md`](HISTORY.md)に記録する。

AviUtl2 v2.10公開SDKにはシーン一覧列挙、シーン切り替え、シーン追加、各シーン長の取得APIがない。現在シーンのID・名前取得と変更通知だけが利用可能である。非公開内部APIや画面操作の模倣には依存しない。保存済み`.aup2`の読み取り専用解析と、現在のRootへのシーンオブジェクト配置は対応可能であり、[`SCENE_LEARNING.md`](SCENE_LEARNING.md)の手順を使う。

AviUtl2 v2.10公開SDKには既存オブジェクトの終了フレームまたは長さを直接変更するAPIもない。削除してエイリアスから再生成する代替方式は、連結、中間点、外部プラグイン固有情報を損なう可能性があるため採用しない。安全な公開APIが利用できるまで長さ変更は未対応とする。

公開SDKには複数オブジェクトのグループ・連結関係を取得するAPIがない。プロジェクトファイルに存在する`group=`は単一オブジェクトの`get_object_alias`から除外されることも実機確認した。未保存変更を反映できないプロジェクトファイルの直接解析や、配置からの推測は行わず未対応とする。取得できる`TRACK_INFO.group_*`はエフェクト内のトラックバー項目グループとして明確に区別する。

### フィルター追加

全オブジェクト一覧と単一オブジェクト詳細は選択状態に関係なく取得する。未選択オブジェクトを設定変更・移動・複製の対象にするため、一覧のindexを指定する安全確認付きフォーカス移動を使用する。フォーカス移動は最新の`state_token`、事前プレビュー、`apply: true`、SDK編集コールバック内での配置・内容再検証、変更後の再取得確認を必須とし、Undo対象外のUI操作として扱う。

AviUtl2 v2.10公開SDKには既存オブジェクトへエフェクトを追加するAPIがない。対象内部へ追加する場合は、元エイリアスへフィルターブロックを追加し、元を削除して同じ位置へ再生成する。`repeat_effect`による空き位置検証と、`replace_source`による同位置置換を実装・実機確認済みである。生成後の詳細比較を必須とし、不一致時は元エイリアスから復元する。公開SDKで取得できない複数オブジェクト間のグループ・連結情報を持つ可能性がある対象へは無条件に適用しない。

これとは別に、`フィルタオブジェクト`を対象の直下レイヤーへ独立配置し、重なる時間範囲へ作用させる構造を確認した。独立配置と通常オブジェクト内部の色調補正を比較し、初期値・利用項目・値形式・トラック形式は共通で、独立配置では通常オブジェクトの代わりに`フィルタオブジェクト`が所有本体になるだけであることを確認した。したがって、独立状態から学習したフィルターブロックは、`フィルタオブジェクト`本体を除外すれば通常オブジェクト内部への追加にもそのまま使用する。

非初期値の色調補正を持つフィルタオブジェクト、対象の赤い円、内部に色調補正を持つ赤い円は、それぞれエイリアス再生成の詳細比較に成功した。生成後の詳細比較は両方式とも必須とする。ただし、公開SDKから独立フィルタオブジェクトの作用対象を示す明示的な関連情報は取得できないため、直上以外、複数候補、複数レイヤー、時間範囲の作用規則は追加検証してから一般機能化する。個別の生成ブロック、設定項目、値形式、確認状態は[`ALIAS_CATALOG.md`](ALIAS_CATALOG.md)を正本とする。

### オブジェクト・フィルターの学習方針

生成用エイリアスの学習は、標準オブジェクト、標準フィルター、外部フィルタープラグインの順に進める。単純な標準オブジェクトから開始し、音声、グループ制御、カメラ制御、中間点、外部ファイル参照、複数オブジェクト間の関係を持つ可能性がある対象は後段で個別に検証する。

2026-07-23時点で、ユーザーが提示した標準オブジェクト、カスタムオブジェクト、標準フィルターの初期値取得・空き位置への生成比較を一巡し、ユーザー確認により初回学習対象をすべて完了した。以後は外部フィルタープラグインの学習へ進む。個別の確認済みブロックと未確認範囲は[`ALIAS_CATALOG.md`](ALIAS_CATALOG.md)を正本とする。

各対象について、全項目が初期値の最小サンプルと、変更可能な項目を初期値以外にしたサンプルを一組として用意する。AviUtl2本体、SDK、外部プラグインの名称とバージョンも記録し、更新による項目名、初期値、値の正規化処理の変化を区別できるようにする。

検証は次の順序で統一する。

1. 元オブジェクトの情報とエイリアスを取得する。
2. レイヤーとフレーム範囲が重複しない空き位置へ再生成する。
3. 種類、素材、エフェクト順序、全設定値、有効・ロック状態、中間点、トラック情報を元と比較する。
4. 非初期値が保持されることを確認する。
5. フィルター追加を扱う場合は、同位置置換と失敗時の復元も確認する。
6. 名前だけではなくエフェクト順序・インデックスでも対象を識別し、同名フィルターを複数配置した状態で個別変更できることを確認する。
7. 結果を[`ALIAS_CATALOG.md`](ALIAS_CATALOG.md)へ記録し、実機確認の履歴を[`HISTORY.md`](HISTORY.md)へ記録する。

カタログでは、少なくとも「取得確認済み」「空き位置への生成確認済み」「非初期値の保持確認済み」「同位置置換確認済み」「設定変更確認済み」「要補正」「未対応」を区別する。生成時に値が変化した対象は成功例として登録せず、バージョン情報と実測値を添えて「要補正」または「未対応」とする。

例えば、Codexが取得結果を基に「現在のカーソル位置には、背景画像、立ち絵、字幕、セリフ音声の4つのオブジェクトがあります」と正しく説明できれば、読み取りインターフェースの基礎が成立したと判断する。

### 将来の拡張

読み取り機能とデータ形式が安定した後に、次の編集操作を検討する。

- オブジェクトの複製
- シーンの追加
- オブジェクトの移動
- 長さやパラメーターの変更
- 既存シーンから必要な要素だけを抽出して複製
- 変更内容の事前確認
- 複数の変更を一回のUndoで戻す機能

特に有力な用途は、既存シーンから背景、固定立ち絵、装飾、画面効果などを選び、新しいシーンの土台として複製する操作である。

### 構想のまとめ

アプリ側はAIの判断を行わない。AviUtl2の状態と操作手段を外部へ正しく提供し、具体的な利用方法はAI向け仕様書とユーザーの指示に委ねる。

まずはシーン、カーソル、選択状態、標準オブジェクトという基本情報を取得できるところから始める。取得可能な情報とデータ形式を確認しながら、その後の用途をユーザー側で広げていく。

## プロジェクト構成

- `Aul2MIRAI.dpr`: `InitializePlugin`、`RegisterPlugin`、`UninitializePlugin` をexportする入口。
- `Aul2MIRAI.dproj`: Delphi Win64 Debug / Releaseビルド設定。ビルド後にDLLを `.aux2` へコピーする。
- `ALIAS_CATALOG.md`: サンプルから学習したオブジェクト・フィルターの生成用エイリアス、値形式、確認状態。
- `EFFECT_LEARNING.md`: 基準素材、フィルター設定値、キャプチャ比較から確認した実際の映像変化。
- `Setup\make_release_zip.bat`: Release配置済みファイルを収集し、同じフォルダの`Aul2MIRAI.zip`へまとめる入口。
- `Setup\make_release_zip.ps1`: 配布対象一覧、存在確認、一時パッケージ作成、ZIP圧縮を担当する。将来の配布物は`packageFiles`へ追加する。
- `Source\Aul2MIRAIPlugin.pas`: `AI MIRAI` の編集メニュー、クライアントウィンドウ、表示リソースを管理する。
- `Source\Aul2MIRAIView.pas`: 最新の外部操作ログ1行、短い状態、ヘルプだけを表示するコンパクトなWin32画面。デバッグ用の説明、オブジェクト全文、詳細ログは表示しない。
- `Source\Aul2MIRAIObjectTypes.pas`: SDKポインターを含まないシーン・オブジェクトのスナップショット型。
- `Source\Aul2MIRAIObjectReader.pas`: 1回のread section内で全レイヤーをオブジェクト単位に探索する取得処理。
- `Source\Aul2MIRAIObjectEquivalence.pas`: エイリアス再生成したオブジェクトについて、配置に依存しない設定・中間点・エフェクト状態・トラック情報の同一性を比較する。
- `Source\Aul2MIRAIObjectAlias.pas`: UTF-8エイリアスのコピー・解析と、既存エフェクトブロックの繰り返し生成。
- `Source\Aul2MIRAIObjectFormat.pas`: 取得結果を確認画面用テキストへ整形する処理。
- 外部通信関連ユニットの構成は[`PIPE_INTERFACE.md`](PIPE_INTERFACE.md)に記載する。
- `Source\Aul2MIRAILog.pas`: 件数制限付きの操作ログ。
- `Source\Lib\AviUtl2Plugin\plugin2.h`: AviUtl2 v2.10公式SDKヘッダー。SDK仕様の正本として参照する。
- `Source\Lib\AviUtl2Plugin\AviUtl2PluginTypes.pas`: `Aul2AudioFilter`を参考にしたAviUtl2汎用プラグインSDKのDelphi型定義。新しいAPIを追加するときは `plugin2.h` と照合する。
- `Source\Lib\AviUtl2Plugin\AviUtl2PluginCore.pas`: 編集ハンドル、プロジェクトハンドル、編集状態取得を共有するDelphi共通ユニット。必要な機能を実装する段階でビルド対象へ追加する。
- `Source\Lib\FFmpeg\FFmpegApi.pas`: FFmpeg 8.1系DLLの型定義、関数ポインタ、動的ロード処理。
- `Source\Lib\FFmpeg`: FFmpeg 8.1.1のAPI定義、実行時DLL、ライセンス文書。詳細は同フォルダの `README.md` を参照する。
- `Win64\Debug` / `Win64\Release`: DelphiのDCUなどの中間出力。
- `C:\ProgramData\aviutl2\Plugin\Aul2MIRAI`: Debug / Releaseのプラグイン出力先。

## ユニット分割方針

- `Aul2MIRAI.dpr` はAviUtl2のexport境界と登録開始・解除だけを担当する。
- `Aul2MIRAIPlugin.pas` はウィンドウ登録とプラグイン全体のライフサイクルを担当する。
- 新しい機能が独立した責務を持つ場合だけ、`Source\Aul2MIRAIXxx.pas` として分割する。
- 複数機能から再利用できる処理だけを `Source\Lib` へ移す。プロジェクト固有処理は安易にライブラリ化しない。
- `uses` と `.dproj` の `DCCReference` には、実際のビルドで必要なユニットだけを登録する。

## ビルド方法

Delphi 37.0を使用する。

Debug Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\Aul2MIRAI\Aul2MIRAI.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Release Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\Aul2MIRAI\Aul2MIRAI.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

出力先:

```text
C:\ProgramData\aviutl2\Plugin\Aul2MIRAI\Aul2MIRAI.aux2
```

- ビルド前イベントで出力フォルダを作り、生成された `Aul2MIRAI.dll` を同じフォルダの `Aul2MIRAI.aux2` にコピーする。
- Debugでは `.dll` と `.aux2` を残す。Releaseでは `.aux2` の生成後に `.dll` と `.rsm` を削除する。
- AviUtl2へ配置するファイルは `.aux2` とする。
- Release確定前に、警告とエラーがないこと、および `.aux2` が最新DLLと同一内容であることを確認する。
- 必須exportは `InitializePlugin`、`RegisterPlugin`、`UninitializePlugin` の3つとする。

Release ZIP:

```powershell
Setup\make_release_zip.bat
```

- Releaseビルド後に実行し、`Setup\Aul2MIRAI.zip`を生成する。
- ZIP内は`Aul2MIRAI`フォルダをルートとし、`Aul2MIRAI.aux2`、`README.md`、`AI_USAGE.md`、`PIPE_INTERFACE.md`、`ALIAS_CATALOG.md`、`EFFECT_LEARNING.md`を含める。
- 将来README、ライセンス、DLLなどが必要になった場合は、`Setup\make_release_zip.ps1`の`packageFiles`へ追加する。

## FFmpeg

- VideoMinerからコピーした `FFmpegApi.pas` を低レベルAPI境界として使う。
- FFmpeg機能を有効にするまでは `Aul2MIRAI.dpr` と `.dproj` のビルド対象へ追加しない。
- 現在のAPIユニットは、`avutil-60.dll`、`swresample-6.dll`、`swscale-9.dll`、`avcodec-62.dll`、`avformat-62.dll`、`avfilter-11.dll` をロードする。
- FFmpeg DLLは `Aul2MIRAI.aux2` と同じフォルダへ配置する。`Source\Lib` やサブフォルダのままではロードされない。
- FFmpegのレコード配置と関数宣言はFFmpeg 8.1系DLLに対応している。DLLのメジャーバージョンだけを入れ替えない。
- FFmpeg呼び出しから返されたフレーム、パケット、コンテキストは、対応するFFmpeg APIで確実に解放する。
- デコードはAviUtl2のUIスレッドを長時間ブロックしない構成にする。終了処理ではデコード処理を止めてからDLL利用リソースを解放する。
- FFmpegを同梱して配布するときは `Source\Lib\FFmpeg\LICENSE` と配布元の説明を確認し、必要なライセンス文書を同梱する。

## AviUtl2実機確認

- AviUtl2が `Aul2MIRAI.aux2` を正常に読み込むことを確認する。
- プラグイン情報、編集メニュー、クライアントウィンドウの表示名がすべて `AI MIRAI` であることを確認する。
- 編集メニューからウィンドウを表示できることを確認する。
- AviUtl2終了時とプラグイン解除時に例外やアクセス違反が発生しないことを確認する。
- 実機確認が終わるまでは、コンパイル成功だけで完成扱いにしない。

## コメントルール

- コメントは、処理を読めば分かることではなく、目的、責務、注意点、状態の意味を補うために書く。
- 古い仕様や現在の実装と食い違うコメントは、見つけた時点で更新する。
- 不要なコメントや重複したコメントを増やしすぎない。
- `var` ブロック内にローカル関数やローカル手続きを入れ子にしない。必要な補助処理は同じ `implementation` 内の独立した関数・手続きとして分ける。
- ユニット先頭には、そのユニットの目的や担当範囲を `//` コメントで記述する。
- フィールドや定数のコメントは右側に1行で置き、同じブロック内では `:`、`=`、`//` の位置を揃える。
- レコード定義ではフィールド名、`:`、型名、行末の `//` の位置を揃え、各フィールドの用途や値の意味を書く。
- コメントと対象の宣言・実装の間には空行を入れない。
- `interface` に公開する `procedure` / `function` には、呼び出し側から見た責務、入出力、重要な副作用を宣言直前の `//` コメントで書く。
- `property`、`procedure`、`function` 宣言は、横幅112文字以内に収まる場合は折り返さない。
- プロジェクトファイルと全ユニット（`.dpr`、`.dproj`、`.pas`）は、常にUTF-8 BOM付きで保存する。新規作成時や編集時もこの形式を維持し、ANSIやBOMなしUTF-8へ戻さない。Delphiの文字コード判定の揺れによるコメントやGUI表示の文字化けを防ぐため、作業完了前にBOMの有無を確認する。

## 保守ルール

- `README.md` には利用者向けの情報だけを置き、細かい開発メモを増やさない。
- `note.md` には作業再開時に必要な、現在有効な情報だけを置く。
- 完了済みの経緯や検証ログは `HISTORY.md` に移し、`note.md` を肥大化させない。
- コピーした `Source\Lib` のコードを変更する場合は、変更理由とAviUtl2 SDKとの互換性を確認する。
- SDK境界の関数シグネチャ、レコード配置、アラインメント、呼出規約を推測で変更しない。
- 未使用ユニット、古いコピー、ビルド対象外コードをプロジェクト内へ残さない。
