# VOICEVOXプロジェクト連携学習

VOICEVOXへ台本を渡し、話者とスタイルが設定済みの編集可能なプロジェクトを開くための学習記録。対象は`D:\VOICEVOX`に配置されたVOICEVOX 0.25.2である。

## 目的

利用者が次のような簡単な台本を書く。

```text
ずんだもん（通常）：読み上げテストを始めるのだ。
ずんだもん（喜）：やった、うまくいったのだ！
四国めたん（怒）：もう、本当に怒ったわ！
```

AI MIRAI側は台本を解析して一時的なVOICEVOXプロジェクト`.vvproj`を生成し、VOICEVOXへ起動引数として渡す。利用者はVOICEVOX上で読み、アクセント、イントネーションなどを調整してから音声を書き出す。

## 責任範囲

AI MIRAI側の責任は次までとする。

1. 台本から話者、抽象感情、セリフ本文を取得する。
2. 話者と抽象感情をVOICEVOXの`voice`へ変換する。
3. 編集可能な一時`.vvproj`を生成する。
4. `VOICEVOX.exe <一時プロジェクト>`として開く。
5. 生成、起動、変換警告、失敗を利用者へ通知する。

次はAI MIRAI側で扱わない。

- イントネーションやアクセントの自動確定。
- WAV、TXT、LABの自動生成。
- 新旧朗2の監視開始、セリフ登録、AviUtl2への配置。
- 新旧朗2またはAviUtl2側のオブジェクト操作。

利用者がVOICEVOXから設定済みフォルダへ書き出した後は、新旧朗2の既存機能へ任せる。

## 採用した流れ

```text
話者（通常・喜・怒・哀・楽）：セリフ形式の台本
  ↓
台本解析と話者・感情の検証
  ↓
VOICEVOXのspeakerId・styleIdへ変換
  ↓
一時.vvprojを生成
  ↓
VOICEVOX.exe 一時.vvproj
  ↓
話者・スタイル・セリフ設定済みで開く
  ↓
利用者が読み・アクセント・イントネーションを調整
  ↓
利用者が指定フォルダへ音声を書き出す
  ↓
新旧朗2へ任せる
```

## 不採用にした方法

### クリップボード貼り付け

`話者（スタイル）,セリフ`を複数行で貼り付けても、VOICEVOXは話者指定として解析しない。各行全体が現在選択中の話者のセリフになった。

### TXTを起動引数へ渡す

`VOICEVOX.exe <テキストファイル.txt>`ではVOICEVOX自体は起動するが、TXTは自動インポートされなかった。VOICEVOX 0.25.2ではTXTの読み込みは画面上の「テキスト読み込み」またはドラッグ＆ドロップが必要である。

TXT方式は話者指定を含む台本をVOICEVOXへ渡せるが、利用者の読み込み操作が1回必要になるため、起動だけで完成する連携には採用しない。

### VOICEVOX Engineによる直接合成

同梱`D:\VOICEVOX\vv-engine\run.exe`はHTTP APIを公開し、`/audio_query`と`/synthesis`から音声を生成できる。0.25.2のローカルエンジンで、ずんだもん・ノーマルのRIFF WAV、モノラル、24000Hzをメモリ上へ生成できることを確認した。

ただし利用者がVOICEVOX上でイントネーションを調整する今回の目的とは異なる。直接合成は将来、新旧朗2側で一括音声生成を実装する場合の候補とし、AI MIRAIの今回の責任には含めない。

## 入力台本の基本形式

```text
話者（感情）：セリフ
```

例:

```text
ずんだもん（通常）：読み上げテストを始めるのだ。
ずんだもん（喜）：やった、うまくいったのだ！
ずんだもん（怒）：もう、本当に怒ったのだ！
ずんだもん（哀）：うまくいかなくて悲しいのだ……。
四国めたん（通常）：読み上げテストを始めます。
四国めたん（怒）：もう、本当に怒ったわ！
```

解析時の基本規則:

- 行内で最初に現れる`：`または`:`を話者部と本文の区切りにする。
- 区切り以降のコロンは本文として保持する。
- 話者部末尾の全角または半角括弧を抽象感情として読む。
- 感情省略時は`通常`とする。
- 空行は無視する。
- 不明な話者、感情、空本文は推測で確定せず警告対象にする。
- 元台本は変更しない。
- 変換前に、行番号、話者、抽象感情、実スタイル、本文を一覧表示できる構成を優先する。

## 抽象感情

利用者はVOICEVOX固有のスタイル名ではなく、次の5種類を指定する。

| 入力 | 意味 |
| --- | --- |
| `通常` | 標準的な読み方 |
| `喜` | 嬉しい、喜んでいる |
| `怒` | 怒っている、強い反発 |
| `哀` | 悲しい、弱っている |
| `楽` | 楽しい、陽気 |

話者ごとに利用可能なスタイルが異なるため、抽象感情から実スタイルへの対応表は話者単位で持つ。適切な専用スタイルがない場合は、不自然なスタイルを推測せず`ノーマル`へフォールバックし、変換警告へ残す。

入力の表記揺れ候補:

| 正規形 | 入力候補 |
| --- | --- |
| `通常` | `通常`、`普通`、`普` |
| `喜` | `喜`、`喜び`、`嬉しい` |
| `怒` | `怒`、`怒り`、`激怒` |
| `哀` | `哀`、`悲`、`悲しい` |
| `楽` | `楽`、`楽しい`、`陽気` |

本文だけから感情をAIが推測する機能は別機能とし、最初の実装では利用者が明示した感情だけを変換する。

## 確認済みVOICEVOX識別情報

エンジン:

```text
engineId = 074fc39e-678b-4c13-8916-ffca8d505d1d
```

### ずんだもん

```text
speakerId = 388f246b-8c41-4ac1-8e2d-5d79f3ff56d9
```

| スタイル | styleId |
| --- | ---: |
| ノーマル | 3 |
| あまあま | 1 |
| ツンツン | 7 |
| セクシー | 5 |
| ささやき | 22 |
| ヒソヒソ | 38 |
| ヘロヘロ | 75 |
| なみだめ | 76 |

暫定感情対応:

| 抽象感情 | 実スタイル | styleId | 状態 |
| --- | --- | ---: | --- |
| 通常 | ノーマル | 3 | 実機確認済み |
| 喜 | あまあま | 1 | 実機確認済み |
| 怒 | ツンツン | 7 | 実機確認済み |
| 哀 | なみだめ | 76 | 実機確認済み |
| 楽 | あまあま | 1 | 暫定。同じ`喜`との使い分けは要試聴 |

### 四国めたん

```text
speakerId = 7ffcb7ce-00ec-4bdc-82cd-45a8889e43ff
```

| スタイル | styleId |
| --- | ---: |
| ノーマル | 2 |
| あまあま | 0 |
| ツンツン | 6 |
| セクシー | 4 |
| ささやき | 36 |
| ヒソヒソ | 37 |

暫定感情対応:

| 抽象感情 | 実スタイル | styleId | 状態 |
| --- | --- | ---: | --- |
| 通常 | ノーマル | 2 | プロジェクト生成形式を確認済み |
| 喜 | あまあま | 0 | 暫定。要試聴 |
| 怒 | ツンツン | 6 | プロジェクト生成形式を確認済み |
| 哀 | ヒソヒソ | 37 | 暫定。`ささやき`との比較が必要 |
| 楽 | あまあま | 0 | 暫定。同じ`喜`との使い分けは要試聴 |

## `.vvproj`の確認済み構造

VOICEVOX 0.25.2の保存形式はUTF-8 JSONであり、最上位に`appVersion`、`talk`、`song`が必要である。

```json
{
  "appVersion": "0.25.2",
  "talk": {
    "audioKeys": [],
    "audioItems": {}
  },
  "song": {
    "tpqn": 480,
    "tempos": [{ "position": 0, "bpm": 120 }],
    "timeSignatures": [
      { "measureNumber": 1, "beats": 4, "beatType": 4 }
    ],
    "tracks": {},
    "trackOrder": []
  }
}
```

`song`は今回使用しないが、0.25.2のプロジェクトスキーマで必須である。実際の生成時は空の既定トラックを1件含める。

セリフ1件の最小構造:

```json
{
  "text": "セリフ本文",
  "voice": {
    "engineId": "074fc39e-678b-4c13-8916-ffca8d505d1d",
    "speakerId": "388f246b-8c41-4ac1-8e2d-5d79f3ff56d9",
    "styleId": 3
  }
}
```

- `talk.audioKeys`へセリフ順に一意な文字列キーを並べる。
- `talk.audioItems`へ同じキーでセリフ本体を登録する。
- `audioKeys`の全キーは`audioItems`に存在しなければならない。
- 各セリフには`voice.engineId`、`voice.speakerId`、`voice.styleId`が必要である。
- `query`、`presetKey`、`morphingInfo`は省略可能である。
- `query`を省略してもVOICEVOXで正常に開き、利用者が通常どおりイントネーションを調整できた。
- 同一の`speakerId`を使い、セリフごとに`styleId`だけを変える構成が正常に読み込まれた。

## 2026-08-04 実機確認

### TXT起動引数

- 2件の話者指定を含む一時TXTを作成した。
- `D:\VOICEVOX\VOICEVOX.exe <一時TXT>`でVOICEVOXは起動した。
- TXTは自動インポートされなかった。
- 検証用TXTは削除した。

### 最小プロジェクト

- `query`を持たない2件の`audioItem`を含む一時`.vvproj`を外部生成した。
- ずんだもん・ノーマルと四国めたん・ツンツンを指定した。
- `VOICEVOX.exe <一時.vvproj>`からプロジェクトが正常に開いた。
- 話者、スタイル、セリフ本文が正しく反映されたことを利用者が確認した。
- ウィンドウタイトルも一時プロジェクト名を示し、VOICEVOX 0.25.2がプロジェクトとして読み込んだことを確認した。

### 同一話者のスタイル切り替え

- 同じずんだもんの`speakerId`を持つ4件を作成した。
- 各件の`styleId`をノーマル`3`、あまあま`1`、ツンツン`7`、なみだめ`76`へ変更した。
- 4件が同じ話者の別スタイルとして正常に開いた。
- 感情が正しく切り替わっていることを利用者が確認した。
- 検証後、VOICEVOXを終了し、一時`.vvproj`を削除した。

## 一時プロジェクトの扱い

- 作成先はユーザーが明示した一時フォルダまたは安全なアプリ用一時領域とする。
- 既存`.vvproj`を上書きしない。
- ファイル名は衝突しない一意な名前にする。
- VOICEVOXが読み込む前に削除しない。
- 利用者が通常のプロジェクトとして保存する場合は、VOICEVOXの「名前を付けて保存」を使用する。
- AI MIRAIの終了時に無条件削除するとVOICEVOX編集中のファイルを失う可能性があるため、削除時期を管理する。
- 起動に失敗した一時ファイルは、原因確認に必要な間だけ保持し、利用者へ場所を伝える。

## バージョン互換性

- 今回確認した正本はVOICEVOX 0.25.2である。
- `.vvproj`の`appVersion`は有効なセマンティックバージョンでなければならない。
- 現在のVOICEVOXより新しい`appVersion`のファイルは警告対象になる。
- VOICEVOX更新時は、同版の公式スキーマまたはVOICEVOX自身が保存したサンプルを確認してから生成形式を更新する。
- 話者とスタイルは追加・変更される可能性があるため、可能なら起動中のVOICEVOX Engine `/speakers`から取得し、静的対応表だけへ依存しない。
- 取得できない場合は、確認済みバージョンに対応する静的情報を使用し、対象バージョン不一致を警告する。

## 未確認事項

- 四国めたんの`喜`、`哀`、`楽`に最も適するスタイルの試聴比較。
- ずんだもんの`楽`を`あまあま`と`ノーマル`のどちらへ割り当てるか。
- 数百件規模の台本を1プロジェクトへ入れた場合の起動時間と操作性。
- VOICEVOXがすでに起動中の場合に、未保存編集を保護しながら一時プロジェクトを渡す手順。
- 不明話者、不明スタイル、VOICEVOX未起動・未配置、エンジン不足時の利用者向けエラー表示。
- 一時ファイルの保存場所、保持期間、回収方法。

## 参照

- [VOICEVOX公式サイト](https://voicevox.hiroshiba.jp/)
- [VOICEVOXの使い方](https://voicevox.hiroshiba.jp/how_to_use/)
- [VOICEVOXエディター公式リポジトリ](https://github.com/VOICEVOX/voicevox)
- [VOICEVOX Engine公式リポジトリ](https://github.com/VOICEVOX/voicevox_engine)

